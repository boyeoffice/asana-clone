<template>
  <div
    :class="{
      'single-blog-post mt-4 mt-lg-0': true,
      'style-2': post.style2,
      'style-1': post.style1,
      'style-4': post.style4,
      'd-flex': post.dFlex,
      'mb-5': post.mb5,
      'mb-lg-4': post.mb4
    }"
  >
    <div :class="{ 'blog-thumbnail': true, 'bg-overlay': post.overlay }">
      <nuxt-link :to="post.url">
        <div v-lazy-container="{ selector: 'img' }">
          <img :data-src="post.img" :alt="post.title" :class="[post.height]" />
        </div>
      </nuxt-link>
    </div>
    <div class="blog-content">
      <span class="post-date">
        {{ post.date }}
      </span>
      <nuxt-link :to="post.url" class="post-title">
        {{ post.title }}
      </nuxt-link>
      <a href="javascript:void(0);" v-if="post.author" class="post-author">
        By {{ post.author }}
      </a>
    </div>
  </div>
</template>

<script lang="ts">
import Vue from 'vue'
export default Vue.extend({
  name: 'BlogList',
  props: {
    post: Object
  }
})
</script>
